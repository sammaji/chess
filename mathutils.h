#include "chutils.h"

#ifndef mathutils_h
#define mathutils_h

int max(int x, int y);
int min(int x, int y);

int dist(point *p1, point *p2);
int stlgdist(point *p1, point *p2);
int stsmdist(point *p1, point *p2);

#endif
